# Cosas para el apache
Esto es para recordar lo que hice para apache.

## Lo realizado hasta ahora:
- Primero se crea un .htacces en la raiz del proyecto.
- Dentro del .htaccess se escribe (en este caso) "ErrorDocument 404 "GitHub/des_app_web/_apache/404.html" y se crea un 404.html en la ruta especificada para los errores 404 (URL errónea).